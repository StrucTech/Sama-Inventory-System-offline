# إصلاح مشكلة عدم ظهور التقرير السابق

## المشكلة الأساسية:
بعد إنشاء تقرير والعودة لإنشاء تقرير آخر، لم تكن معلومات التقرير السابق تظهر.

## الأسباب المكتشفة:

### 1. **البحث عن نوع واحد من التقارير فقط** ❌
**المشكلة:** الكود كان يبحث عن ملفات تبدأ بـ "تقرير_شامل_" فقط
**التأثير:** التقارير المفلترة الجديدة (تقرير_مفلتر_) لم تكن تظهر

**الحل:** ✅
```python
# قبل الإصلاح
report_files = [f for f in os.listdir(reports_path) 
               if f.startswith(f"تقرير_شامل_{self.project_name}") and f.endswith('.xlsx')]

# بعد الإصلاح  
report_files = [f for f in os.listdir(reports_path) 
               if (f.startswith(f"تقرير_شامل_{self.project_name}") or 
                   f.startswith(f"تقرير_مفلتر_{self.project_name}")) and f.endswith('.xlsx')]
```

### 2. **عدم قراءة هيكل التقارير المفلترة** ❌
**المشكلة:** التقارير المفلترة لها هيكل مختلف من الأوراق
**التأثير:** فشل في قراءة التواريخ من التقارير المفلترة

**الحل:** ✅
```python
# إضافة دعم لأوراق متعددة
sheet_names = ['جميع الحركات', 'الحركات المفلترة', 'معلومات التقرير']

# قراءة التواريخ من ورقة معلومات التقرير للتقارير المفلترة
if report_type == "مفلتر":
    info_df = pd.read_excel(file_path, sheet_name='معلومات التقرير', engine='openpyxl')
    # استخراج التواريخ باستخدام regex
    dates = re.findall(r'\d{4}-\d{2}-\d{2}', info_text)
```

### 3. **عدم التحديث بعد إنشاء تقرير جديد** ❌
**المشكلة:** معلومات التقرير السابق لم تكن تتحدث بعد إنشاء تقرير جديد
**التأثير:** المستخدم لا يرى أن تقريره الجديد أصبح "السابق"

**الحل:** ✅
```python
# إضافة تحديث بعد نجاح التصدير
if filepath:
    # تحديث معلومات التقرير السابق
    self.load_last_report_info()
    
# إضافة تحديث عند فتح النافذة مرة أخرى
def showEvent(self, event):
    super().showEvent(event)
    self.load_last_report_info()
```

### 4. **تمييز نوع التقرير** ✅
**التحسين:** إضافة عرض نوع التقرير (شامل/مفلتر)
```python
report_type = "مفلتر" if "تقرير_مفلتر_" in latest_file else "شامل"
f"التقرير السابق ({report_type}) كان من {start_date} إلى {end_date}"
```

## النتيجة النهائية:

### ✅ **التحسينات المطبقة:**
1. **دعم جميع أنواع التقارير**: شامل ومفلتر
2. **قراءة ذكية للتواريخ**: من أوراق مختلفة حسب نوع التقرير  
3. **تحديث فوري**: بعد إنشاء تقرير جديد
4. **تحديث تلقائي**: عند فتح النافذة مرة أخرى
5. **عرض نوع التقرير**: تمييز بين الشامل والمفلتر
6. **معالجة أخطاء محسنة**: للحالات الاستثنائية

### 🎯 **الآن النظام يعمل كالتالي:**
1. إنشاء تقرير أول → يُحفظ بنجاح
2. فتح نافذة التقرير مرة أخرى → **يظهر التقرير السابق** ✅
3. إنشاء تقرير ثاني → يُحفظ ويُحدث معلومات التقرير السابق فوراً
4. فتح النافذة مرة ثالثة → **يظهر التقرير الثاني كسابق** ✅

**المشكلة محلولة بالكامل! 🎉**