# نظام التنبيهات: المخزون المنخفض والصلاحية

## التحسين المُطبق

تم تحويل قسم "معلومات المشروع" إلى **"تنبيهات"** يحتوي على جدولين مهمين للمراقبة والتنبيه:

### 1. جدول المخزون المنخفض 📊

#### الوظيفة:
عرض العناصر التي وصلت كميتها إلى الحد الأدنى المسموح به.

#### المحتويات:
| العمود | الوصف |
|--------|--------|
| **العنصر** | اسم العنصر |
| **الكمية الحالية** | الكمية المتوفرة حالياً |
| **الحد الأدنى** | الحد الأدنى المطلوب (افتراضياً 10) |

#### منطق العمل:
```python
# التحقق من المخزون المنخفض
for item in inventory:
    current_qty = item['الكمية_الحالية']
    min_qty = 10  # حد أدنى افتراضي
    
    if current_qty <= min_qty and current_qty > 0:
        # إضافة للتنبيهات
        add_to_low_stock_alert(item)
```

#### التمييز البصري:
- 🔴 **خلفية حمراء** للصفوف التي تحتاج تنبيه
- **نص أبيض** للوضوح
- **إخفاء تلقائي** إذا لم توجد تنبيهات

### 2. جدول المواد قاربة انتهاء الصلاحية ⏰

#### الوظيفة:
عرض **كل دفعة دخول على حدة** التي تقارب انتهاء صلاحيتها (10 أيام أو أقل).

#### المحتويات:
| العمود | الوصف |
|--------|--------|
| **العنصر** | اسم العنصر |
| **الكمية** | كمية هذه الدفعة تحديداً |
| **تاريخ الدخول** | متى دخلت هذه الدفعة |
| **الأيام المتبقية** | كم يوم متبقي على انتهاء الصلاحية |

#### منطق العمل المتقدم:
```python
# تتبع كل دفعة دخول منفردة
for transaction in incoming_transactions:
    shelf_life_days = transaction['مدة_الصلاحية_بالأيام']
    entry_date = transaction['التاريخ']
    
    if shelf_life_days > 0:
        expiry_date = entry_date + timedelta(days=shelf_life_days)
        days_remaining = (expiry_date - datetime.now()).days
        
        # تنبيه إذا بقي 10 أيام أو أقل
        if days_remaining <= 10 and days_remaining >= 0:
            add_to_expiry_alert(transaction, days_remaining)
```

#### مثال عملي:
```
العنصر: دواء أ
- دفعة 1: دخلت 1/1/2024 بكمية 100 (صلاحية 70 يوم) ← باقي 5 أيام
- دفعة 2: دخلت 15/2/2024 بكمية 50 (صلاحية 70 يوم) ← باقي 20 يوم

النتيجة: يظهر تنبيه للدفعة الأولى فقط (100 وحدة، باقي 5 أيام)
```

#### التمييز البصري:
- 🔴 **أحمر** للعناصر المنتهية خلال 3 أيام (خطر عالي)
- 🟡 **أصفر** للعناصر المنتهية خلال 4-10 أيام (تحذير)
- **ترتيب تلقائي** حسب الأولوية (الأقل أيام أولاً)

## التحسينات التقنية

### 1. هيكل الكود الجديد:

#### أ) تحديث الواجهة:
```python
def create_project_info(self):
    """إنشاء منطقة التنبيهات"""
    # تغيير العنوان من "معلومات المشروع" إلى "تنبيهات"
    alerts_title = QLabel("تنبيهات")
    
    # جدول المخزون المنخفض
    self.low_stock_table = QTableWidget()
    
    # جدول الصلاحية
    self.expiry_table = QTableWidget()
```

#### ب) دوال التحديث المنفصلة:
```python
def update_project_info(self):
    """تحديث التنبيهات"""
    self.update_low_stock_alerts()    # المخزون المنخفض
    self.update_expiry_alerts()       # الصلاحية
```

### 2. خوارزمية تتبع الصلاحية المتقدمة:

#### المشكلة السابقة:
كان النظام يتعامل مع المخزون ككتلة واحدة، فإذا دخل 100 وحدة في يناير و 50 وحدة في مارس، كان يعرض تنبيه للكمية الإجمالية.

#### الحل الجديد:
```python
# تتبع كل معاملة دخول منفردة
incoming_transactions = df[df['نوع_العملية'] == 'دخول']

for transaction in incoming_transactions:
    # حساب انتهاء الصلاحية لهذه الدفعة تحديداً
    entry_date = transaction['التاريخ']
    shelf_life = transaction['مدة_الصلاحية_بالأيام']
    
    expiry_date = entry_date + timedelta(days=shelf_life)
    days_remaining = (expiry_date - datetime.now()).days
```

### 3. الذكاء في العرض:

#### إخفاء تلقائي:
```python
# إخفاء الجداول إذا لم توجد تنبيهات
if len(alerts) == 0:
    table.setVisible(False)
else:
    table.setVisible(True)
```

#### ترتيب ذكي:
```python
# ترتيب حسب الأولوية
expiry_items.sort(key=lambda x: x['days_remaining'])  # الأقل أيام أولاً
```

## التصميم والألوان

### 1. نظام الألوان:
- 🔴 **إطار أحمر** للتنبيهات العامة
- 🟡 **خلفية صفراء** للتحذيرات المتوسطة
- 🔴 **خلفية حمراء** للتنبيهات العاجلة
- ⚪ **نص أبيض** للوضوح

### 2. التصميم المرئي:
```css
QFrame#alerts_frame {
    border: 2px solid #e74c3c;  /* إطار أحمر */
    border-radius: 10px;
}

QLabel#alerts_title {
    color: #e74c3c;              /* نص أحمر */
    background-color: #ffe6e6;   /* خلفية وردية فاتحة */
}
```

## سيناريوهات الاستخدام

### 1. سيناريو المخزون المنخفض:
```
الحالة: سكر متبقي 8 كيلو، الحد الأدنى 10
التنبيه: يظهر في جدول المخزون المنخفض
العمل المطلوب: طلب مخزون إضافي
```

### 2. سيناريو الصلاحية:
```
الحالة: 
- دواء أ: دفعة من 15/1/2024 (100 حبة) صلاحية 60 يوم → باقي 3 أيام
- دواء أ: دفعة من 10/2/2024 (200 حبة) صلاحية 60 يوم → باقي 28 يوم

التنبيه: يظهر الدفعة الأولى فقط (100 حبة، باقي 3 أيام)
العمل المطلوب: استخدام الدفعة الأولى أولاً
```

## الفوائد المحققة

### 1. منع الفاقد:
- **تنبيه مبكر** لانتهاء الصلاحية
- **تتبع دقيق** لكل دفعة على حدة
- **ترتيب أولويات** الاستخدام

### 2. إدارة أفضل للمخزون:
- **مراقبة مستمرة** للكميات
- **تنبيه فوري** عند النفاد
- **تخطيط أفضل** للشراء

### 3. واجهة محسنة:
- **معلومات مهمة** في المقدمة
- **ألوان تحذيرية** واضحة
- **تحديث تلقائي** مع كل عملية

## مقارنة قبل وبعد

### قبل التحسين:
- معلومات إحصائية عامة
- لا توجد تنبيهات
- صعوبة في معرفة المشاكل

### بعد التحسين:
✅ **تنبيهات فورية** للمخزون المنخفض  
✅ **تتبع دقيق** لصلاحية كل دفعة  
✅ **ألوان تحذيرية** واضحة  
✅ **ترتيب حسب الأولوية**  
✅ **إخفاء تلقائي** عند عدم وجود مشاكل  

النظام الآن يوفر **مراقبة استباقية** للمخزون والصلاحية، مما يساعد في **منع المشاكل قبل حدوثها**! 🚀📊