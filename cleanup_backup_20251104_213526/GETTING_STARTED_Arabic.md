# دليل البدء السريع - نظام إدارة المخزون

## دليل الإعداد السريع

تطبيق Python لإدارة المخزون جاهز! اتبع هذه الخطوات للبدء:

### 1. إعداد Google Sheets API (مطلوب)

1. **اذهب إلى Google Cloud Console**: https://console.cloud.google.com/
2. **أنشئ مشروعاً جديداً** أو اختر مشروعاً موجوداً
3. **فعّل Google Sheets API**:
   - اذهب إلى "APIs & Services" > "Library"
   - ابحث عن "Google Sheets API"
   - انقر على "Enable"
4. **أنشئ حساب خدمة**:
   - اذهب إلى "APIs & Services" > "Credentials"
   - انقر على "Create Credentials" > "Service Account"
   - املأ تفاصيل حساب الخدمة
   - انقر على "Create and Continue"
   - تخطى منح وصول المستخدمين (انقر "Done")
5. **أنشئ بيانات الاعتماد**:
   - انقر على البريد الإلكتروني لحساب الخدمة
   - اذهب إلى تبويب "Keys"
   - انقر على "Add Key" > "Create new key"
   - اختر تنسيق "JSON"
   - حمّل الملف

### 2. تثبيت بيانات الاعتماد

1. **انسخ ملف JSON المحمّل** إلى `config/credentials.json`
2. **اكتب البريد الإلكتروني لحساب الخدمة** من ملف JSON (يبدو مثل: `your-service@project-id.iam.gserviceaccount.com`)

### 3. إعداد جداول جوجل

1. **أنشئ مستند جداول جوجل جديد** أو استخدم مستنداً موجوداً
2. **شارك المستند** مع البريد الإلكتروني لحساب الخدمة:
   - انقر على "Share" في جداول جوجل
   - أضف البريد الإلكتروني لحساب الخدمة
   - امنحه صلاحيات "Editor"
3. **اكتب اسم جدولك** (ستحتاج هذا للإعدادات)

### 4. إعداد التطبيق

1. **عدّل `config/config.json`**:
   ```json
   {
       "spreadsheet_name": "اسم الجدول الفعلي هنا",
       "worksheet_name": "المخزون"
   }
   ```

### 5. التحقق من الإعداد

شغّل نص التحقق من الإعداد:
```bash
python setup_check.py
```

هذا سيتحقق من صحة جميع الإعدادات.

### 6. تشغيل التطبيق

```bash
python main_arabic.py
```

## ما يفعله التطبيق

- **عرض المخزون**: يعرض جميع العناصر في جدول منظم
- **إضافة العناصر**: إضافة عناصر مخزون جديدة مع الاسم والكمية والسعر
- **تعديل الكميات**: تحديث كميات العناصر الموجودة
- **حذف العناصر**: حذف العناصر من المخزون
- **المزامنة التلقائية**: جميع التغييرات تتزامن مع جداول جوجل في الوقت الفعلي

## استكشاف الأخطاء

**"فشل الاتصال"**: 
- تحقق من اتصالك بالإنترنت
- تحقق من صحة credentials.json
- تأكد من مشاركة الجدول مع حساب الخدمة

**"ModuleNotFoundError"**:
- شغّل: `pip install -r requirements.txt`

**"لم يتم العثور على الجدول"**:
- تحقق من اسم الجدول في config/config.json
- تأكد من مشاركته مع حساب الخدمة

## تحتاج مساعدة؟

1. شغّل `python setup_check.py` لتشخيص المشاكل
2. تحقق من README_Arabic.md للحصول على وثائق مفصلة
3. تحقق من إعداد Google Sheets API

---

**جاهز لبدء إدارة مخزونك!** 🎉