name: Build and Release Sama Inventory System

on:
  push:
    tags:
      - 'v*'  # يتم تشغيله عند إنشاء tag يبدأ بـ v (مثل v1.0.0)
  workflow_dispatch:  # يمكن تشغيله يدوياً من GitHub Actions

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements_build.txt
        
    - name: Check Files Before Build
      run: |
        echo "📁 Checking project files..."
        dir
        echo "🔍 Looking for main file..."
        if (Test-Path "main_with_auth.py") { echo "✅ main_with_auth.py found" } else { echo "❌ main_with_auth.py missing" }
        if (Test-Path "requirements_build.txt") { echo "✅ requirements_build.txt found" } else { echo "❌ requirements_build.txt missing" }
        if (Test-Path "build_setup.py") { echo "✅ build_setup.py found" } else { echo "❌ build_setup.py missing" }
        
    - name: Build Executable
      run: |
        $env:PYTHONIOENCODING="utf-8"
        $env:PYTHONUTF8=1
        python build_setup.py
        
    - name: Create ZIP Archive
      run: |
        cd dist
        7z a -tzip "../Sama-Inventory-System-${{ github.ref_name }}.zip" "نظام إدارة المخزون"
        
    - name: Get Release Info
      id: release_info
      run: |
        if ("${{ github.ref_name }}" -match "^v(.+)$") {
          $version = $matches[1]
          echo "version=$version" >> $env:GITHUB_OUTPUT
          echo "release_name=Sama Inventory System v$version" >> $env:GITHUB_OUTPUT
        }
        
    - name: Create Release and Upload Asset
      uses: softprops/action-gh-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.ref_name }}
        name: ${{ steps.release_info.outputs.release_name }}
        body: |
          🎉 **نسخة جديدة من نظام إدارة المخزون Sama**
          
          ## ✨ المميزات الجديدة:
          - تحسينات في الأداء
          - إصلاح الأخطاء
          - ميزات جديدة
          
          ## 📦 طريقة التثبيت:
          1. حمل الملف `Sama-Inventory-System-${{ github.ref_name }}.zip`
          2. استخرج المحتويات
          3. شغل `نظام إدارة المخزون.exe`
          
          ## 🔄 التحديث التلقائي:
          إذا كان لديك نسخة سابقة، ستحصل على إشعار تحديث تلقائي عند فتح البرنامج.
          
          ---
          الإصدار: ${{ steps.release_info.outputs.version }}
          تاريخ البناء: ${{ github.event.head_commit.timestamp }}
        files: ./Sama-Inventory-System-${{ github.ref_name }}.zip
        draft: false
        prerelease: false
        
    # إشعار بنجاح البناء (اختياري)
    - name: Notify Success
      if: success()
      run: |
        echo "✅ تم بناء ونشر الإصدار ${{ github.ref_name }} بنجاح!"
        echo "🔗 الرابط: https://github.com/StrucTech/Sama-Inventory-System/releases/tag/${{ github.ref_name }}"