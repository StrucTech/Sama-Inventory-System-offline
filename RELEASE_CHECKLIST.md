# 📋 Release Checklist - قائمة التحقق قبل الإطلاق

استخدم هذه القائمة قبل كل Release للتأكد من أن كل شيء جاهز.

---

## ✅ قبل الإطلاق (Pre-Release)

- [ ] **اختبر البرنامج محلياً**
  ```bash
  python main.py
  ```
  - [ ] جميع المميزات تعمل
  - [ ] لا توجد رسائل خطأ

- [ ] **حدث requirements.txt** (إذا أضفت مكتبات جديدة)
  ```bash
  pip freeze > requirements.txt
  ```

- [ ] **حدث VERSION.txt**
  - [ ] أغير الرقم من `1.0.0` إلى `1.0.1` (مثلاً)

- [ ] **أضف رسالة Commit واضحة**
  ```bash
  git commit -m "Release v1.0.1: Fixed bugs, added features"
  ```

- [ ] **اختبر البناء محلياً** (اختياري لكن مهم)
  ```bash
  python build_exe.py
  dist\SamaInventorySystem.exe
  ```

---

## 🚀 الإطلاق الفعلي (Release)

- [ ] **أضف جميع التغييرات**
  ```bash
  git add .
  ```

- [ ] **احفظ التغييرات**
  ```bash
  git commit -m "Release v1.0.1"
  ```

- [ ] **انشئ Tag جديد** (هذا الأهم!)
  ```bash
  git tag -a v1.0.1 -m "Release Version 1.0.1"
  ```

- [ ] **ارفع التغييرات**
  ```bash
  git push origin main
  ```

- [ ] **ارفع الـ Tag** (يشغل الـ Build)
  ```bash
  git push origin v1.0.1
  ```

---

## ⏳ بعد الإطلاق (Post-Release)

- [ ] **راقب الـ Build**
  - اذهب إلى: https://github.com/StrucTech/Sama-Inventory-System/actions
  - [ ] انتظر حتى ينتهي البناء (عادة 5-10 دقائق)
  - [ ] تأكد من عدم وجود أخطاء ❌

- [ ] **تحقق من Release**
  - اذهب إلى: https://github.com/StrucTech/Sama-Inventory-System/releases
  - [ ] يجب أن يكون Release جديد موجود
  - [ ] [ ] ملف `SamaInventorySystem.exe` موجود
  - [ ] العنوان والوصف صحيحين

- [ ] **أضف وصف مفصل** (إذا لم يكن موجود)
  ```markdown
  # Release v1.0.1
  
  ## ✨ المميزات الجديدة
  - ميزة 1
  - ميزة 2
  
  ## 🐛 الأخطاء المصححة
  - خطأ 1
  - خطأ 2
  
  ## 📥 التحميل
  اضغط أسفل لتحميل SamaInventorySystem.exe
  ```

- [ ] **أخبر المستخدمين**
  - [ ] أرسل البشرى! 🎉

---

## 🔢 نسخ إصدارات (Versioning Pattern)

```
v1.0.0 ← الأول
v1.0.1 ← إصلاح صغير
v1.1.0 ← ميزة جديدة
v1.2.0 ← تحسينات
v2.0.0 ← تحديث رئيسي
```

---

## ⚠️ الأخطاء الشائعة والحلول

| الخطأ | السبب | الحل |
|------|------|------|
| ❌ "Tag already exists" | استخدام نفس الرقم | استخدم رقم مختلف: `v1.0.2` |
| ❌ "Build failed" | مشكلة في الكود | شاهد الخطأ في Actions ثم أصلح |
| ❌ "Nothing to commit" | لا توجد تغييرات | أضف تغييرات حقيقية قبل Commit |
| ❌ "Permission denied" | خطأ في Git | تأكد من أن لديك Push permissions |
| ✅ "Build passed" | كل شيء تمام | اذهب للـ Releases وشاهد النتيجة |

---

## 🎯 أوامر سريعة (Copy & Paste)

### الطريقة السريعة (Everything at once):

```bash
cd "d:\StrucTech Projects\Inventory System - offline"

# 1. التحضير
git add .
git commit -m "Release v1.0.1"
git tag -a v1.0.1 -m "Release v1.0.1"

# 2. الرفع
git push origin main
git push origin v1.0.1

echo ✅ انتهى! اذهب للـ GitHub بعد دقائق
```

### الطريقة الآمنة (خطوة بخطوة):

```bash
# 1. تحديث محلي
git status
git add .
git status

# 2. احفظ
git commit -m "Release v1.0.1"
git log -1

# 3. انشئ Tag
git tag -a v1.0.1 -m "Release v1.0.1"
git tag -l

# 4. ارفع
git push origin main
git push origin v1.0.1

# 5. تحقق
echo Done! Check GitHub.
```

---

## 📊 قائمة التتبع النهائية

قبل كل Release، اطبع أو نسخ هذا:

```
Release v1.0.X Checklist:
- [ ] اختبر التطبيق
- [ ] حدث requirements.txt
- [ ] حدث VERSION.txt
- [ ] git add .
- [ ] git commit -m "Release v1.0.X"
- [ ] git tag -a v1.0.X -m "Release v1.0.X"
- [ ] git push origin main
- [ ] git push origin v1.0.X
- [ ] انتظر 5-10 دقائق
- [ ] شاهد Release على GitHub
- [ ] احتفل! 🎉
```

---

**بارك الله فيك! الآن أنت جاهز للإطلاق! 🚀**
