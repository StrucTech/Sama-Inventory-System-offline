# ميزة فحص الاتصال بالإنترنت

## نظرة عامة
تم إضافة نظام فحص الاتصال بالإنترنت قبل تشغيل التطبيق لضمان عمل جميع الميزات التي تعتمد على الإنترنت.

## الميزات الجديدة

### 🔍 فحص الاتصال التلقائي
- يتم فحص الاتصال بالإنترنت قبل بدء التطبيق
- في حالة عدم وجود اتصال، يظهر تنبيه للمستخدم
- إمكانية إعادة المحاولة أو إغلاق التطبيق

### ⚠️ رسائل التنبيه
- رسالة واضحة عند عدم وجود اتصال
- نصائح لحل مشكلة الاتصال:
  - التأكد من الاتصال بالإنترنت
  - فحص إعدادات الشبكة
  - التحقق من جدار الحماية

### 🔄 فحص متكرر
- فحص الاتصال عند تحديث التحليلات
- فحص عند محاولة الوصول لـ Google Sheets
- إعادة محاولة تلقائية مع إمكانية الإلغاء

## طرق التشغيل

### الطريقة الأولى: ملف البرنامج الرئيسي
```cmd
python main_with_auth.py
```

### الطريقة الثانية: ملف الدفعة البسيط
```cmd
start_inventory_system.bat
```

### الطريقة الثالثة: نافذة التحليل مباشرة
```cmd
python analytics_window.py
```

## آلية عمل الفحص

1. **فحص DNS**: محاولة الاتصال بـ Google DNS (8.8.8.8:53)
2. **فحص HTTP**: محاولة فتح google.com كبديل
3. **المهلة الزمنية**: 3 ثوان لكل محاولة
4. **النتيجة**: إما نجح الاتصال أو فشل

## نقاط الفحص

- **عند بدء التطبيق**: فحص إجباري قبل فتح نافذة تسجيل الدخول
- **عند إنشاء نافذة التحليل**: فحص اختياري مع إمكانية المتابعة
- **عند تحديث البيانات**: فحص قبل محاولة تحميل بيانات جديدة

## رسائل الخطأ

### رسالة البداية
```
⚠️ لا يوجد اتصال بالإنترنت!

يرجى التأكد من:
• الاتصال بالإنترنت
• إعدادات الشبكة  
• جدار الحماية

اضغط 'إعادة المحاولة' للتحقق مرة أخرى
أو 'إلغاء' للخروج من التطبيق
```

### رسالة التحديث
```
⚠️ لا يوجد اتصال بالإنترنت!

يرجى التأكد من الاتصال ثم حاول مرة أخرى.
```

## فوائد هذه الميزة

1. **تجنب الأخطاء**: منع أخطاء الاتصال المفاجئة
2. **تجربة أفضل**: إرشاد المستخدم لحل مشكلة الاتصال
3. **توفير الوقت**: عدم انتظار timeout طويل
4. **وضوح**: رسائل واضحة باللغة العربية

## ملاحظات تقنية

- الفحص لا يؤثر على أداء التطبيق
- يتم الفحص في الخلفية بسرعة
- لا يحتاج صلاحيات إدارية خاصة
- يعمل مع جميع أنواع الاتصالات (WiFi, Ethernet, Mobile)