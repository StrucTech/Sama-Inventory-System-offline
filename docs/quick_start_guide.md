# دليل البداية السريع - Sama Inventory System

## 🚀 إنشاء أول إصدار

### خطوة 1: رفع الكود للـ Repository

```bash
# تهيئة Git (إذا لم يكن مهيأ)
git init
git add .
git commit -m "Initial commit: Sama Inventory System"

# ربط بالـ Repository
git remote add origin https://github.com/StrucTech/Sama-Inventory-System.git
git branch -M main
git push -u origin main
```

### خطوة 2: إنشاء أول إصدار

```bash
# طريقة سريعة
create_release.bat

# سيطلب منك:
# - رقم الإصدار (مثال: v1.0.0)
# - وصف الإصدار (اختياري)
```

### خطوة 3: انتظار البناء التلقائي

1. اذهب إلى: https://github.com/StrucTech/Sama-Inventory-System/actions
2. تابع عملية البناء (تستغرق 5-10 دقائق)
3. عند الانتهاء، ستجد النسخة المستقلة في: https://github.com/StrucTech/Sama-Inventory-System/releases

## 🔄 عملية التطوير والنشر

### عند إضافة ميزة جديدة:

1. **تطوير الكود:**
   ```bash
   # تعديل الكود حسب الحاجة
   # اختبار التغييرات محلياً
   python main_with_auth.py
   ```

2. **إنشاء إصدار جديد:**
   ```bash
   create_release.bat
   # أدخل رقم الإصدار الجديد (مثال: v1.0.1)
   # أدخل وصف التحديثات
   ```

3. **النشر التلقائي:**
   - GitHub Actions سيبني النسخة المستقلة تلقائياً
   - ستكون متاحة في Releases خلال دقائق
   - جميع المستخدمين سيحصلون على إشعار تحديث

## 📦 ما يحدث تلقائياً:

### عند إنشاء Tag جديد:
1. **GitHub Actions** يبدأ عملية البناء
2. **PyInstaller** ينشئ ملف `.exe` مستقل
3. **النسخة المستقلة** تُرفع للـ Releases
4. **المستخدمون** يحصلون على إشعار تحديث

### للمستخدمين النهائيين:
1. **فتح البرنامج** ← فحص تلقائي للتحديثات
2. **إشعار بالتحديث** ← عرض ملاحظات الإصدار
3. **تحميل تلقائي** ← تطبيق التحديث
4. **إعادة تشغيل** ← استخدام النسخة الجديدة

## 🎯 مثال عملي:

```bash
# أنت طورت ميزة جديدة
git add .
git commit -m "إضافة ميزة البحث المتقدم"

# إنشاء إصدار جديد
create_release.bat
# أدخل: v1.1.0
# أدخل: إضافة ميزة البحث المتقدم وتحسينات الأداء

# النتيجة:
# - الكود يُرفع لـ GitHub
# - Tag v1.1.0 يُنشأ
# - GitHub Actions يبني النسخة المستقلة
# - النسخة تظهر في Releases
# - المستخدمون يحصلون على التحديث تلقائياً
```

## 🔗 الروابط المهمة:

- **Repository**: https://github.com/StrucTech/Sama-Inventory-System
- **Releases**: https://github.com/StrucTech/Sama-Inventory-System/releases
- **Actions**: https://github.com/StrucTech/Sama-Inventory-System/actions
- **Issues**: https://github.com/StrucTech/Sama-Inventory-System/issues

## ⚡ نصائح للتطوير:

1. **اختبر محلياً** قبل إنشاء إصدار جديد
2. **استخدم أرقام إصدارات منطقية** (v1.0.0, v1.0.1, v1.1.0)
3. **اكتب وصف واضح** للتحديثات
4. **تابع عملية البناء** في GitHub Actions
5. **اختبر النسخة المستقلة** قبل توزيعها

## 🎉 الخلاصة:

الآن لديك نظام كامل للتطوير والنشر:
- ✅ **تطوير محلي** سهل ومرن
- ✅ **بناء تلقائي** للنسخة المستقلة
- ✅ **توزيع تلقائي** عبر GitHub
- ✅ **تحديثات تلقائية** للمستخدمين
- ✅ **إدارة إصدارات** احترافية

**بدء العمل:** شغل `create_release.bat` وأنشئ أول إصدار! 🚀